.next
out
build
coverage
pnpm-lock.yaml

# Vendor/generated assets, not hand-authored — reformatting just churns diffs
assets/font/
public/favicon/site.webmanifest
components/nearlock-app/Content/*.json

# Exact byte-for-byte form is owned by firstmate's fm-ensure-agents-md.sh convention;
# Prettier's markdown formatter would insert a blank line after the HTML comment.
CLAUDE.md
